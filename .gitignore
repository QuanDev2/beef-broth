# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Env / secrets  (NEVER commit real keys)
.env
.env.*
!.env.example

# Sample photos (test fixtures — keep local, don't bloat the repo)
sandbox/photos/

# OS
.DS_Store
