# Copy this file to ".env" in the repo root, then paste your real key in.
# .env is git-ignored, so your key stays local and never gets committed.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
